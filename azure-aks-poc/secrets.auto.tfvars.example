# Copy to secrets.auto.tfvars (gitignored) and fill in real values:
#   cp secrets.auto.tfvars.example secrets.auto.tfvars
#
# Terraform auto-loads any *.auto.tfvars file, so no extra -var-file flag
# needed once this is filled in and renamed.

db_password   = "CHANGE_ME"
ghcr_username = "RaghvDevOps"
ghcr_token    = "ghp_CHANGE_ME"
