# Terraform local artifacts
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json

# Account-specific backend config -- generated by scripts/bootstrap_backend.sh
backend.hcl

# Secrets -- real values, never commit
secrets.auto.tfvars
*.pem
*.key

# Debug/terminal output captures -- may contain account IDs, IPs, etc.
outputs/

# OS/editor noise
.DS_Store
.vscode/
